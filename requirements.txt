# Runtime dependencies for the TwinGrid API (Python 3.12).
#
# Pinning policy: exact pins (==) are used ONLY where this repo carries
# evidence of a working combination -- models/optimizer/ppo_model.zip records
# that it was produced with stable-baselines3 2.9.0 + gymnasium 1.3.0 on
# Python 3.12. Everything else uses a tested-major-compatible range.
# To freeze the exact set you have verified, run in a clean 3.12 venv:
#     pip install -r requirements.txt && pip freeze > requirements.lock
# (streamlit / plotly / matplotlib were removed: nothing in the API imports
# them. src/lstm_model.py imports matplotlib lazily for optional plots only.)

# API & server
fastapi>=0.115,<1.0
uvicorn>=0.30,<1.0
websockets>=12.0

# Data & ML
pandas>=2.2,<4
numpy>=1.26,<3
scikit-learn>=1.4,<2
tensorflow>=2.18,<3

# RL
stable-baselines3==2.9.0
gymnasium==1.3.0

# Database
sqlalchemy[asyncio]>=2.0.30,<2.1
asyncpg>=0.29,<1
alembic>=1.13,<2

# Auth (passlib removed: it is unmaintained and breaks with bcrypt>=4.1)
python-jose[cryptography]>=3.3,<4
bcrypt>=4.0.1,<6

# Ingestion
paho-mqtt>=2.0,<3
requests>=2.31,<3
openpyxl>=3.1,<4

# Utils
python-dotenv>=1.0,<2
tqdm>=4.66,<5

# Rate limiting & observability
slowapi>=0.1.9,<1
prometheus-client>=0.19,<1
