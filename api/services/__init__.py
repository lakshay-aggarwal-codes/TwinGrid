"""Business-logic layer for the Digital Twin API -- owns the singleton
DigitalTwin/JointOptimizer/AnomalyDetector instances and the logic that
uses them, independent of HTTP concerns (routes) and persistence
(repositories)."""